#include<bits/stdc++.h>
using namespace std;
#define rep(i, n) for(int i=0; i < (int)(n) ; i++)

int main(){
    int n,L;
    int k;
    cin >> n >> L;
    cin >> k;
    vector<int> a(n);
    rep(i,n) cin >> a[i];

    vector<int> piece(k+1);
    
    return 0;
}
